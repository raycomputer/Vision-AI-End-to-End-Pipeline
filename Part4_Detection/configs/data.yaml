# 데이터셋 경로 (절대 경로 또는 상대 경로)
path: ./datasets  # 데이터셋 루트 디렉토리 (train_yolo.py 위치 기준 상위 폴더 참조)

# 학습(train)과 검증(val) 이미지 경로 (path 기준 상대 경로)
train: images/train
val: images/val

# 클래스 정보
nc: 2              # 클래스 개수 (고양이, 개 2개라면)
names: ['cat', 'dog']  # 클래스 이름 (0번: cat, 1번: dog)